[pytest]
pythonpath = src
# anywr/test_app.py is a standalone self-check ("DB=... python test_app.py"), not a
# pytest suite: it collects nothing and its imports made a bare `pytest` here fail.
testpaths = tests
